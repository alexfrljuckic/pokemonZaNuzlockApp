# Copy to .env.local (gitignored) and fill in. See supabase/README.md.
# The app must work fully with VITE_SYNC_ENABLED unset/false — no backend exists
# by default, and datasets/engine already work entirely offline via IndexedDB.
VITE_SYNC_ENABLED=false
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
